# PyTorch installed separately with the CUDA index on the VM:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Keep that out of this file so pip install -r requirements.txt stays index-agnostic.

# Geospatial
rasterio>=1.3
geopandas>=0.14
shapely>=2.0
pyproj>=3.6

# Data / numerics
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3

# Modeling
segmentation-models-pytorch>=0.3
timm>=1.0                # backbones for smp (EfficientNet-Bx, ResNet)
albumentations>=1.4
opencv-python-headless>=4.7,<4.11  # pin: 4.11 typing/__init__.py references cv2.dnn.DictValue which is absent in NGC base cv2
matplotlib>=3.8          # training/visualizations figures

# Experiment tracking
mlflow[gcs]>=2.10,<3.0  # 3.x ties model registry to tracking URI; GCS not a supported registry backend

# GCS
google-cloud-storage>=2.14
gcsfs>=2025.0

# Config / utils
pyyaml>=6.0
tqdm>=4.66

# Tests
pytest>=7.4
