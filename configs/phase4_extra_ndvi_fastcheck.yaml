# Stage 0.1 gate-neutral check: rerun the finished phase4_extra_ndvi (best_smoothed 0.8879 @ ep35,
# seed 42) under base_v2_fast. Same seed/data/aug → identical trajectory; only stops sooner.
# PASS = best_smoothed within σ of 0.8879 (confirms the stop-fix doesn't change the deployed model).
base: base_v2_fast.yaml
data:
  data_root: /outputs/v1.0/data_local
  normalization_stats_path: /outputs/v1.0/staging/v1_splits/normalization_stats_ndvi.json
channels:
  rgb: true
  extra:
    - {name: ndvi, band: 0}
mlflow:
  run_name: phase4_extra_ndvi_fastcheck
