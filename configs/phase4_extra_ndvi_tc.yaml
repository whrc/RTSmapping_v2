# Greedy forward channel selection (round 1): NDVI + tasseled-cap (tc_1, tc_2). base_v2_fast,
# boundary none + pf0.5 inherited (apples-to-apples vs the NDVI-alone anchor 0.8985).
base: base_v2_fast.yaml
seed: 42
data:
  data_root: /outputs/v1.0/data_local
  normalization_stats_path: /outputs/v1.0/staging/v1_splits/normalization_stats_ndvi_tc.json
channels:
  rgb: true
  extra:
    - {name: ndvi, band: 0}
    - {name: tc_1, band: 6}
    - {name: tc_2, band: 7}
mlflow:
  run_name: phase4_extra_ndvi_tc
