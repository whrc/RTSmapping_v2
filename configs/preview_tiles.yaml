# Fixed preview tiles for cross-experiment visual comparison (3 pos + 3 neg).
#
# Explicit tile UIDs from the v1.0 **val_realistic** split. Pinning them means every
# experiment/seed renders the SAME tiles → preview grids are directly comparable.
#
# ⚠ Regenerated 2026-06-21: the previous set (2026-06-14) pre-dated the 2026-06-16
# region-leakage split regeneration, so 5 of its 6 UIDs were no longer in val_realistic
# → every run silently rendered a **1-tile** preview instead of 3-pos/3-neg
# (log: "Preview tiles not in val split (skipped): [...] Using 1 fixed preview tiles").
# These UIDs are verified members of the current val_realistic split (98 pos / 2,053 neg)
# and are staged in /mnt/outputs/v1.0/data_local. `_select_preview_tiles` now falls back to
# the seeded heuristic if fewer than (wanted-2) tiles match, so a future split change can't
# silently degrade the panel again.
#
# Regenerate via: get_tile_ids("val_realistic", metadata, splits, class_filter) on the
# current splits.yaml (see data/splits.py); pick 3 pos + 3 neg that are staged.
positive:
  - b7zfm1zzrcpx
  - ch66f5pzrcpw
  - zkwefr84bn85
negative:
  - b4tpwjj2tgpv
  - ck4y1psue18p
  - zs94f0p1qgpv
