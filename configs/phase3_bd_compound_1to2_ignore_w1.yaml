# §6.2 boundary factorial — compound(focal+dice,1:2) × boundary ignore width 1.
# One-level inherit of baseline + compound loss + boundary deltas. Local-staged data (§13).
base: phase0c_seed42.yaml
data:
  data_root: /outputs/v1.0/data_local
loss:
  function: compound
  lambda_focal: 1.0
  lambda_dice: 2.0
  boundary_handling: ignore
  boundary_ignore_width: 1
mlflow:
  run_name: phase3_bd_compound_1to2_ignore_w1
